# Generated by build-fixtures.js — committed copies of the CSS so the
# Playwright static server can serve them at /css/*. Regenerate with
# `node src/test/e2e/build-fixtures.js` after editing real CSS files.
css/
