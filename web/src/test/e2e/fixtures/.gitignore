# Generated by build-fixtures.js — copies of the real CSS and JS so the
# Playwright static server can serve them at /css/* and /js/*. Regenerate
# with `node src/test/e2e/build-fixtures.js` after editing real assets.
css/
js/

# magic.html is NOT ignored: it's the real template's render, refreshed via
#   ./gradlew :web:test --tests '*MagicE2eFixtureTest*' -Dfixture.refresh=true
# and committed so `npm run test:e2e` needs no JVM. That same test fails if
# the committed copy drifts from the template.
