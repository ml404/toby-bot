<configuration>

    <!-- Console Appender for logging to the console during tests -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Set the root logger level to DEBUG and add the console appender -->
    <root level="DEBUG">
        <appender-ref ref="CONSOLE" />
    </root>

    <!-- Optionally, set specific log levels for certain packages -->
    <logger name="toby" level="DEBUG" />
    <logger name="net.dv8tion.jda" level="WARN" />

</configuration>
